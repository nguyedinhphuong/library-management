server:
  port: 8080

spring:
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:123456}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: HikariConnPool

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  redis:
    host: localhost
    port: 6379

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

open:
  api:
    description: "API cho moi truong DEV"
    serverUrl: "http://localhost:8080"
    serverName: "Server DEV"

jwt:
  expiryHour: 1
  expiryDay: 12
  # secretKey: lm2XAzpcGcvO01wfoi24eE2KlSSkFePFR5Z9vcrLWSs=
  # refreshKey: vNeBW9paRM484PJvLWrRSZcGyWuIF6DfU9k/h712iZ0=
  # resetKey: vNeBW9paRM484PJvLWrRSZcGyWuIF6DfU9k/h712iZ0=

